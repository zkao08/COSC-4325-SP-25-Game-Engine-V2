// Classes Data Header
// Contains a list and short description of all items that can be created.

#pragma once

#include <string>
#include <map>

const std::map<std::string, std::string> classes{
	{"Entity", "Base class of all objects."},
	{"Object", "Base class of objects that exist in 2D space."}
};